using UnityEngine;

namespace NoREroMod.Systems.Spawn;

/// <summary>World Z depth captured from the vanilla source when the template was cached.</summary>
internal sealed class SpawnTemplateSpawnSettings : MonoBehaviour
{
    public float SpawnWorldZ;
}
